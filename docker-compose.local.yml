# Local development only — maps a host port to the container.
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up --build
services:
  app:
    container_name: jev-game
    ports:
      - "${PORT:-3000}:3000"
